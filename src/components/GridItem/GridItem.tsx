import { levels } from "../../helpers/imc"; //escri levels e não level ok!

type Props = { 
    item: levels
};

export const GridItem = ({item}: Props) => {
    return (
         <div>...</div>
);
}